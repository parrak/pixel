[project]
name = "asc-rcm-lite"
version = "0.1.0"
description = "Synthetic ASC surgical RCM domain model and ingestion fixtures"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
